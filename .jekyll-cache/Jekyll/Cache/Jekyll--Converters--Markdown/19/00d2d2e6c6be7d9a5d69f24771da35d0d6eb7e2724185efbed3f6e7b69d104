I"�<p>Docker환경에서 YOLOv4를 학습시키려고 한다.<br />
Docker Hub에서 도커 이미지를 가져오려다가 처음부터 만드는 것도 재밌을 것 같아서 삽질 겸 Dockerfile를 만들었다.</p>

<p>`
FROM ubuntu:20.04
USER root</p>

<table>
  <tbody>
    <tr>
      <td>RUN echo “Acquire::Check-Valid-Until "false";\nAcquire::Check-Date "false";”</td>
      <td>cat &gt; /etc/apt/apt.conf.d/10no–check-valid-until</td>
    </tr>
  </tbody>
</table>

<p>RUN apt-get update
RUN apt-get upgrade -y
RUN apt-get install git -y</p>

<p>RUN mkdir /ejkim
RUN git clone https://github.com/AlexeyAB/darknet.git /ejkim
`</p>

<p>처음 <code class="highlighter-rouge">FROM ubuntu:20.04</code> 만</p>

:ET